# Rust's output directory
target
**/target

# Auto-regenerated by `cargo test` (Soroban ledger snapshots) — not source
test_snapshots/
**/test_snapshots/

# Soroban / Stellar local settings
.soroban
.stellar

# Secrets — NEVER commit (oracle signing key lives here)
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# Node
node_modules
dist
.DS_Store
.gstack/
.worktrees/

# Local agent / tooling scratch (never commit)
.claude/
.claude-dev-helper/
.dist/
.context/
.agents/
.cursor/
docs/counterpoint/
docs/internal/
reference/
skills-lock.json

# Local demo video PDF (not part of the public tree)
DEMO-VIDEO-MOTION.pdf

# VS Code MCP config carries the Stitch API key — never commit
.vscode/
